node_modules
# Keep environment variables out of version control
.env
.env.local
.env.production
/generated/prisma
.claude
# SQLite database (user data — never commit)
gamehub.db
*.db-journal
prisma/*.db
prisma/*.db-journal
.next
data
public/covers/
