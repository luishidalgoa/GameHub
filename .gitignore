node_modules
docker-compose.yml
# Keep environment variables out of version control
.env
.env.local
.env.production
/generated/prisma
.claude
# SQLite database (user data — never commit). The repo ships NO .db file;
# they're generated locally / live in the Docker volume. Match every variant
# (gamehub.db, prisma/*.db, gamehub_backup_*.db, dev.db, …).
*.db
*.db-journal
*.db.bak
.next
data
public/covers/
tsconfig.tsbuildinfo
*.db.bak
worker/
